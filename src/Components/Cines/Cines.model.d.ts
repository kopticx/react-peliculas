export interface cine{
    id: number;
    nombre: string;
    ubicacion: Marker;
}

export interface cinesTransferDTO {
    key: string;
    nombre: string;
}

export interface cinesFormularioDTO{
    nombre: string;
    ubicacion: Marker;
}
